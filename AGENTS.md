# TFCoop maintenance

- The user works on this PC concurrently. Do not use computer control or simulate desktop input. The user has explicitly authorized real local TF2 record/replay tests in sequential processes, reversible preparation, and closing those owned test processes. Use tools/local_game_replay.py with its installer journal, launch lease and exact process ownership checks; announce launches. Automatic app.loadGame currently returns false, so use --manual-load and tell the user the exact fresh imported save name. Other development checks remain headless. Never close or modify an unrelated running game.
- Current public repository: https://github.com/TastierPizza-code/TFCoop. Keep current source and releases there when delivering a new version; the user has requested GitHub delivery and startup updates.
- Current launcher entry: probe_launcher.py; implementation: prototype/. Older coop/mod/upstream code is retained for comparison and shared helpers.
- Use prototype/release_version.py for the numeric release tag. Keep launcher display, mod name, guides and release notes consistent.
- Build a public TFCoop-Windows.zip with tools/build_probe_package.py and publish using tools/publish_release.py. Never publish --private-save builds, savegame pairs, session secrets, diagnostics, stock game files, local caches, or user-specific paths.
- Check downloaded release and file hashes before handoff. Do not update a running game/test, downgrade a cached release, or bypass validation on errors.
- Describe runtime evidence accurately. A headless model pass does not prove TF2 determinism, free concurrent construction, or native UI pause synchronization.
- Accepted baseline: v0.5.12 / commit 28c739880870b66ad0ebe59882f8de4c729489aa. The user accepted the actual two-PC run at about 0.962x with barely visible judders. Preserve the released package, tag and stream_v1 comparison path; defer pacing/visual optimization until the remaining co-op mechanics work. See prototype/docs/ACCEPTED_BASELINE.md and prototype/docs/NEXT_MILESTONE.md. Acceptance covers the measured automatic scene, not arbitrary input capture or the complete world.
- Future new test modes must minimize automatic prerequisites so the players reach the new interactive feature quickly. Reuse accepted behavior without repeating the full 240-round build/endurance sequence in every new mode. Keep existing selectable test modes and their full reference sequences unchanged. Implement a separately identified short preparation with its own actual readiness checks and honest proof scope; retain required shared-world, callback and fail-stop checks. Do not shorten BUILD_ROUNDS globally or label a short preparation as the full BuildProof. Apply this to the next new test after evaluating the ongoing Alpha5.13 run; do not interrupt or replace that running test.
- Actual Alpha5.13 two-PC evidence (2026-09-08): the launcher-input subtest passed with 380 byte-identical journal rows, six mutual pause/resume transitions, a 59.297-second joint pause and all seven recorded requests settled through final frame 488. Active pace regressed to about 0.82x; this is not a newly accepted pacing baseline. Same-batch opposite requests, native game UI pause and free construction remain untested in game. Earlier startup failures remain unresolved. See prototype/docs/ALPHA513_EVIDENCE.md; preserve the Alpha5.12 baseline and use a separate short prelude for the next new test.
- The user subsequently explicitly prioritized reducing the Alpha5.13 stutter regression on 2026-09-08. Alpha5.14 adds paced_live_v1 with input seals carried on existing receipts, short_scene_v1 with ten preparation rounds, and coalesced informational launcher statuses. This supersedes the earlier deferral of targeted pacing work. Preserve all three old selectable modes and native/stream pacing. Headless checks do not establish improved actual pacing; the new two-PC run remains outstanding until provided.
- Alpha5.14's real Prepare action exposed a missed integration: probe_install's separate configuration whitelist rejected short_scene_v1 before installation. Alpha5.15 uses shared configuration_semantics/validate_configuration_semantics across staging, installation and startup. Keep test_preparation_pipeline's actual prepare/install/read_setup/restore chain for every mode and both roles; separate mocked component tests or frozen import checks alone do not validate this boundary. The short mode's actual two-PC pacing remains unverified.
- Next-release user requirement (2026-09-08): during the pre-beta testing phase, persist the host's IPv4 and a fixed private pairing code so the friend does not repeatedly copy them. Keep this test profile across startup, update and installation restore; automatic address discovery must not overwrite it. Private host preference is recorded in build/next-test-pairing/preferences.json (excluded from publication). Do not publish the user's address or access code in public source/packages/diagnostics. Separate the persistent pairing identity from each fresh coordinated run identity; connection_identity currently derives network epoch from the code, so merely stopping code regeneration is not the complete change. Preserve fresh native epochs, consumed-session guards, UUID runs, new queues and no reconnect. This is queued for the next version, not implemented in v0.5.15; beta connection UX can be revisited later.
- Current accepted input/pacing reference: the user accepted the actual Alpha5.15 two-PC run on 2026-09-08 and explicitly wants this tempo preserved. Both reports verify 133 byte-identical world rows, ten-round readiness, 182 live advances, seven mutual pause/resume transitions, a 47.375-second joint pause and settled final frame 192. Active rate is 0.939391x/0.937996x; the short prelude is 12.500 seconds. Preserve v0.5.15/commit 3b24380284c82ee0c4199e8eb94b90dc463bf708 and paced_live_v1 as the input reference, alongside the older Alpha5.12 endurance reference. This supersedes the pending two-PC status above. No further pacing experiments unless requested or a concrete regression requires work; carry this feel into the next features. Private verified recovery is build/alpha515-recovery-20260908. See prototype/docs/ACCEPTED_INPUT_BASELINE.md. Acceptance covers the observed short scene, not native UI/free building/full-world determinism.
